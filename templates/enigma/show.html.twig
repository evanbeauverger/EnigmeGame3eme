{% extends 'base.html.twig' %}

{% block title %}Enigme 1{% endblock %}

{% block body %}
<style>
    * { text-align: center; }
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    a { color: blue; }
    body { background-color: yellow; }
    h3 { margin: 50px; }
    h1 { margin: 40px; }
    button { margin: 15px; }
</style>

<head>
    <title>Énigme sur les IA</title>
</head>
{% if app.user %}
    <form action="/enigma">
        <button type="submit" class="btn btn-primary">Retour à la gestion</button>
    </form>
{% endif %}

<h1>{{ enigma.title }}</h1>

<h3>
{{ enigma.instruction }}
</h3>

<form action="/menu_enigmes" method="post">
    <label for="answer">Réponse : </label>
    <input type="text" name="answer" id="answer" required><br/><br/>
    <button type="submit" class="btn btn-primary">Valider</button>
</form>
<form action="/menu_enigmes">
    <button type="submit" class="btn btn-primary">Abandonner</button>
</form>

<img src="{{asset('images/openai.jpg')}}" />

{% endblock %}
