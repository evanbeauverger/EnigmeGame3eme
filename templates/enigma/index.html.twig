{% extends 'base.html.twig' %}

{% block title %}Enigma index{% endblock %}

{% block body %}
    <h1>Liste des énigmes</h1>

    <table class="table">
        <thead>
            <tr>
                <th>N°</th>
                <th>Titre</th>
                <th>Instruction</th>
                <th>Réponse</th>
                <th>Type<th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enigma in enigmas %}
            <tr>
                <td>{{ enigma.order }}</td>
                <td>{{ enigma.title }}</td>
                <td>{{ enigma.instruction }}</td>
                <td>{{ enigma.secretcode }}</td>
                <td>{{ enigma.type.label }}</td>
                <td>
                    <a href="{{ path('app_enigma_show', {'id': enigma.id}) }}">voir</a>
                    <a href="{{ path('app_enigma_edit', {'id': enigma.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_enigma_choose_type') }}">Créer une énigme</a>
    <a href="/teacher/gestion">Retour</a>
{% endblock %}
